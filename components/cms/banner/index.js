const Banner = ({
  primary: {
    banner_title,
    banner_description,
    banner_image,
    banner_button_label,
    banner_button_link,
  },
}) => {
  return (
    <section>
      <h2>Banner</h2>
    </section>
  );
};

export default Banner;
